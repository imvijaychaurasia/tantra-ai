# tantra-media — Python dependencies
# =============================================================================
# Core framework
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2

# TTS — Kokoro TTS (open-source, neural quality, fully local, no API key)
kokoro-onnx>=0.4.0
soundfile>=0.12.1

# Image generation — Pillow for styled slide images
Pillow==10.4.0
numpy>=2.0.2

# HTTP client (for health checks)
httpx==0.27.2

# Async utilities
anyio==4.6.2
