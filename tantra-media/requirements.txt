# tantra-media — Python dependencies
# =============================================================================
# Core framework
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2

# TTS — gTTS (Google Text-to-Speech, free, no API key, works from server IPs)
gTTS>=2.5.0

# Image generation — Pillow for styled slide images
Pillow==10.4.0
numpy>=1.26.4

# HTTP client (for health checks)
httpx==0.27.2

# Async utilities
anyio==4.6.2
